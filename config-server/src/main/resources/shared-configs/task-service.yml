spring:
  data:
    mongodb:
      uri: mongodb://admin:password123@localhost:27017/task_db?authSource=admin
  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: password123

jwt:
  secret: my-super-secret-jwt-key-for-learning-only

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

logging:
  level:
    com.taskplatform.task: DEBUG