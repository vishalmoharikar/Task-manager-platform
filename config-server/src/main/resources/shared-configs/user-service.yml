spring:
  data:
    mongodb:
      uri: mongodb://admin:password123@localhost:27017/user_db?authSource=admin
    redis:
      host: localhost
      port: 6379
      password: password123

jwt:
  secret: my-super-secret-jwt-key-for-learning-only

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html